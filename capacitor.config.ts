import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ai-playing-dino-chrome',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
